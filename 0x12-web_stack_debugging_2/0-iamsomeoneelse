#!/usr/bin/env bash
# Check if exactly one argument is provided
if [ $# -ne 1 ]; then
  echo "Usage: $0 <username>"
  exit 1
fi

# Get the username from the argument
username="$1"

# Use sudo to run whoami as the specified user
sudo -n -u "$username" whoami
